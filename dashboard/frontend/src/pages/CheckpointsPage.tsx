import { SectionCard } from '../components/ui/SectionCard'

export function CheckpointsPage() {
  return (
    <SectionCard title="Checkpoints" subtitle="Checkpoint storage and recovery will be added later.">
      <div className="empty-state">No checkpoint activity</div>
    </SectionCard>
  )
}
