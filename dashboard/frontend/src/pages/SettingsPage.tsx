import { SectionCard } from '../components/ui/SectionCard'

export function SettingsPage() {
  return (
    <SectionCard title="Settings" subtitle="Configuration management will expand as the platform grows.">
      <div className="empty-state">Dashboard settings placeholder</div>
    </SectionCard>
  )
}
